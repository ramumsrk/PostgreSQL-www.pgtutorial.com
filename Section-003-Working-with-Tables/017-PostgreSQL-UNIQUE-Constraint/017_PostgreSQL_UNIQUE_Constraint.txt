********* QUERY **********
SELECT
CURRENT_DATABASE(),
CURRENT_USER,
CURRENT_ROLE,
CURRENT_SCHEMA
**************************

 current_database | current_user | current_role | current_schema 
------------------+--------------+--------------+----------------
 pgtutorial       | pgtutorial   | pgtutorial   | pgtutorial
(1 row)

********* QUERY **********
CREATE TABLE IF NOT EXISTS brands
(
brand_id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
brand_name VARCHAR(255) NOT NULL CONSTRAINT unique_brand_name UNIQUE
)
**************************

CREATE TABLE
********* QUERY **********
SELECT n.nspname as "Schema",
  c.relname as "Name",
  CASE c.relkind WHEN 'r' THEN 'table' WHEN 'v' THEN 'view' WHEN 'm' THEN 'materialized view' WHEN 'i' THEN 'index' WHEN 'S' THEN 'sequence' WHEN 't' THEN 'TOAST table' WHEN 'f' THEN 'foreign table' WHEN 'p' THEN 'partitioned table' WHEN 'I' THEN 'partitioned index' END as "Type",
  pg_catalog.pg_get_userbyid(c.relowner) as "Owner",
  CASE c.relpersistence WHEN 'p' THEN 'permanent' WHEN 't' THEN 'temporary' WHEN 'u' THEN 'unlogged' END as "Persistence",
  am.amname as "Access method",
  pg_catalog.pg_size_pretty(pg_catalog.pg_table_size(c.oid)) as "Size",
  pg_catalog.obj_description(c.oid, 'pg_class') as "Description"
FROM pg_catalog.pg_class c
     LEFT JOIN pg_catalog.pg_namespace n ON n.oid = c.relnamespace
     LEFT JOIN pg_catalog.pg_am am ON am.oid = c.relam
WHERE c.relkind IN ('r','p','t','s','')
  AND c.relname OPERATOR(pg_catalog.~) '^(brands)$' COLLATE pg_catalog.default
  AND pg_catalog.pg_table_is_visible(c.oid)
ORDER BY 1,2;
**************************

                                       List of relations
   Schema   |  Name  | Type  |   Owner    | Persistence | Access method |  Size   | Description 
------------+--------+-------+------------+-------------+---------------+---------+-------------
 pgtutorial | brands | table | pgtutorial | permanent   | heap          | 0 bytes | 
(1 row)

********* QUERY **********
INSERT INTO brands
(brand_name)
VALUES
('Apple')
RETURNING *
**************************

 brand_id | brand_name 
----------+------------
        1 | Apple
(1 row)

INSERT 0 1
********* QUERY **********
INSERT INTO brands
(brand_name)
VALUES
('Apple')
RETURNING *
**************************

********* QUERY **********
CREATE TABLE IF NOT EXISTS product_loans
(
    load_id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    product_id INT NOT NULL,
    user_id INTEGER NOT NULL,
    loan_date DATE NOT NULL,
    return_date DATE,
    UNIQUE (product_id, user_id)
)
**************************

CREATE TABLE
********* QUERY **********
SELECT n.nspname as "Schema",
  c.relname as "Name",
  CASE c.relkind WHEN 'r' THEN 'table' WHEN 'v' THEN 'view' WHEN 'm' THEN 'materialized view' WHEN 'i' THEN 'index' WHEN 'S' THEN 'sequence' WHEN 't' THEN 'TOAST table' WHEN 'f' THEN 'foreign table' WHEN 'p' THEN 'partitioned table' WHEN 'I' THEN 'partitioned index' END as "Type",
  pg_catalog.pg_get_userbyid(c.relowner) as "Owner",
  CASE c.relpersistence WHEN 'p' THEN 'permanent' WHEN 't' THEN 'temporary' WHEN 'u' THEN 'unlogged' END as "Persistence",
  am.amname as "Access method",
  pg_catalog.pg_size_pretty(pg_catalog.pg_table_size(c.oid)) as "Size",
  pg_catalog.obj_description(c.oid, 'pg_class') as "Description"
FROM pg_catalog.pg_class c
     LEFT JOIN pg_catalog.pg_namespace n ON n.oid = c.relnamespace
     LEFT JOIN pg_catalog.pg_am am ON am.oid = c.relam
WHERE c.relkind IN ('r','p','t','s','')
  AND c.relname OPERATOR(pg_catalog.~) '^(product_loans)$' COLLATE pg_catalog.default
  AND pg_catalog.pg_table_is_visible(c.oid)
ORDER BY 1,2;
**************************

                                           List of relations
   Schema   |     Name      | Type  |   Owner    | Persistence | Access method |  Size   | Description 
------------+---------------+-------+------------+-------------+---------------+---------+-------------
 pgtutorial | product_loans | table | pgtutorial | permanent   | heap          | 0 bytes | 
(1 row)

********* QUERY **********
INSERT INTO product_loans
(product_id, user_id, loan_date)
VALUES
(1, 1, '2024-11-23'),
(1, 2, '2024-11-23')
RETURNING product_id, user_id, loan_date
**************************

 product_id | user_id | loan_date  
------------+---------+------------
          1 |       1 | 2024-11-23
          1 |       2 | 2024-11-23
(2 rows)

INSERT 0 2
********* QUERY **********
INSERT INTO product_loans
(product_id, user_id, loan_date)
VALUES
(1, 1, '2024-11-24')
RETURNING product_id, user_id, loan_date
**************************

********* QUERY **********
CREATE TABLE IF NOT EXISTS profiles
(
    user_id INTEGER PRIMARY KEY,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    phone VARCHAR(25) UNIQUE
)
**************************

CREATE TABLE
********* QUERY **********
SELECT n.nspname as "Schema",
  c.relname as "Name",
  CASE c.relkind WHEN 'r' THEN 'table' WHEN 'v' THEN 'view' WHEN 'm' THEN 'materialized view' WHEN 'i' THEN 'index' WHEN 'S' THEN 'sequence' WHEN 't' THEN 'TOAST table' WHEN 'f' THEN 'foreign table' WHEN 'p' THEN 'partitioned table' WHEN 'I' THEN 'partitioned index' END as "Type",
  pg_catalog.pg_get_userbyid(c.relowner) as "Owner",
  CASE c.relpersistence WHEN 'p' THEN 'permanent' WHEN 't' THEN 'temporary' WHEN 'u' THEN 'unlogged' END as "Persistence",
  am.amname as "Access method",
  pg_catalog.pg_size_pretty(pg_catalog.pg_table_size(c.oid)) as "Size",
  pg_catalog.obj_description(c.oid, 'pg_class') as "Description"
FROM pg_catalog.pg_class c
     LEFT JOIN pg_catalog.pg_namespace n ON n.oid = c.relnamespace
     LEFT JOIN pg_catalog.pg_am am ON am.oid = c.relam
WHERE c.relkind IN ('r','p','t','s','')
  AND c.relname OPERATOR(pg_catalog.~) '^(profiles)$' COLLATE pg_catalog.default
  AND pg_catalog.pg_table_is_visible(c.oid)
ORDER BY 1,2;
**************************

                                        List of relations
   Schema   |   Name   | Type  |   Owner    | Persistence | Access method |  Size   | Description 
------------+----------+-------+------------+-------------+---------------+---------+-------------
 pgtutorial | profiles | table | pgtutorial | permanent   | heap          | 0 bytes | 
(1 row)

********* QUERY **********
INSERT INTO profiles
(user_id, first_name, last_name, phone)
VALUES
(1, 'John', 'Doe', NULL),
(2, 'John', 'Doe', NULL)
RETURNING user_id, first_name, last_name, phone
**************************

 user_id | first_name | last_name | phone 
---------+------------+-----------+-------
       1 | John       | Doe       | 
       2 | John       | Doe       | 
(2 rows)

INSERT 0 2
